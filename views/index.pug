doctype html
html(lang="ja")
  head
    meta(charaset="utf-8")
  body
    h1 Messages
    section 
      each msg in messages
        div
          h2 #{msg.username}
          time #{msg.date}
          p #{msg.message}
